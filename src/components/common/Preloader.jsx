import React from 'react';
import preloader from '../../temp/image/preloader.svg';

const Preloader = (props) => {
    return(
        <div>
            <img src={preloader} />
        </div>
    ) 
}

export default Preloader;